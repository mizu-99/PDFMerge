MainRoot:

<MainRoot>:
    CustomSpinner:
        text: 'Home'
        values: 'Home', 'Work', 'Other', 'Custom'
        size_hint: None, None
        size: 100, 44
        pos_hint: {'center_x': .5, 'center_y': .5}
        on_text: root.on_spinner_change(self.text)
